# macOS
.DS_Store

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*secrets*

# Flutter / Dart (applies once the app is scaffolded)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
ios/Pods/
ios/.symlinks/
android/.gradle/
**/generated_plugin_registrant.dart

# IDE
.idea/
*.iml
.vscode/

# Supabase CLI local cache (machine-specific, regenerated by `supabase link`)
supabase/.temp/

# Copyrighted source material used as a content reference — never commit
# third-party copyrighted works into version control. Extracted content
# lives in supabase/migrations/ as our own transcription/adaptation.
*.pdf

# Personal, machine-specific Claude Code permissions — not shared.
.claude/settings.local.json
